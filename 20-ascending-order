public Boolean ascendingOrder(Integer a, Integer b, Integer c) {
    //code here
    Boolean result;
    if(b >= a && c >= b){
        return true;
    }
    else{
        return false;
        return result;
    }
}